/** Slug for the seeded Acme Corp pilot demo organization. */
export const DEMO_ORG_SLUG = "acme-demo";

/** All demo tenants wiped/reseeded by `seed:demo` (multi-org E2E dataset). */
export const DEMO_ORG_SLUGS = ["acme-demo", "beta-demo", "gamma-demo", "delta-demo"] as const;
